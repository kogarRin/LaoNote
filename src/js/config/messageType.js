export const ElMessageConfig = {
    buildConfig: (setType,setMessage,setPlain,setDuration) => ({
        type: setType,
        message: setMessage,
        plain: setPlain,
        duration: setDuration
    })
}
